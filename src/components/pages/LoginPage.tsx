import React from 'react'
import { Text, SafeAreaView } from 'react-native'

export const LoginPage = () => {
    return (
        <SafeAreaView>
            <Text>
                Login Page
            </Text>
        </SafeAreaView>
    )
}